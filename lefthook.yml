pre-commit:
  parallel: false
  commands:
    prettier:
      run: |
        npx prettier --write {staged_files}
        npx prettier --check {staged_files} || exit 1
        git add --force {staged_files}
    eslint:
      run: |
        npx eslint --fix {staged_files}
        npx eslint {staged_files} || exit 1
        git add --force {staged_files}
    tsc:
      run: |
        npx tsc --noEmit
